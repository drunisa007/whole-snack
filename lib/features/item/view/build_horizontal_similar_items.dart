
import 'package:flutter/material.dart';
class BuildHorizontalSimilarItems extends StatelessWidget {
  const BuildHorizontalSimilarItems({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
